# =====================================================
# HOSPITAL IDENTITY (SAME FOR BOTH GROUPS)
# =====================================================
HOSPITAL_ID=Hospital-D
PRODUCT_CODE=PHYSIO-SALINE-500ML
REORDER_THRESHOLD=2.0
DAILY_CONSUMPTION_BASE=52

# =====================================================
# DATABASE CONFIGURATION (SAME FOR BOTH GROUPS)
# =====================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hospital_d_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_MAX_CONNECTIONS=20

# =====================================================
# ARCHITECTURE SELECTION
# =====================================================
# Choose which notification implementation to use:
# - MOCK: For testing without external dependencies (default)
# - SOA: Use SOAP implementation (requires Section B complete)
# - SERVERLESS: Use Event Hub implementation (requires Section C complete)
ARCHITECTURE=MOCK

# =====================================================
# SOAP GROUP - Fill in these values (from Team 1)
# =====================================================
SOAP_WSDL_URL=http://localhost:8000/StockUpdateService?wsdl
SOAP_ENDPOINT=
SOAP_USERNAME=
SOAP_PASSWORD=
SOAP_TIMEOUT=30000

# =====================================================
# SERVERLESS GROUP - Fill in these values (from Team 1)
# =====================================================
EVENT_HUB_CONNECTION_STRING=
EVENT_HUB_INVENTORY_TOPIC=inventory-low-events
EVENT_HUB_ORDER_TOPIC=order-commands
CONSUMER_GROUP=hospital-d-group

# =====================================================
# APPLICATION CONFIGURATION
# =====================================================
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# =====================================================
# MONITORING
# =====================================================
ENABLE_PERFORMANCE_TRACKING=true
